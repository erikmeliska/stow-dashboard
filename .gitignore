# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.
data
# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# Deno desktop shell (generated)
/src-deno/standalone/
/dist/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# Generated data
data/projects_metadata.jsonl
.project_meta.json

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Command Center: skill symlinks are regenerable from skills.manifest.json (solo mode)
.claude/skills/

# Local tool scratch — logs and page dumps, regenerated on every run.
# Untracked-but-not-ignored is how a blanket `git add` once swept a
# 1M-line server.log into a commit.
.paperclip/
.playwright-mcp/
.stow/
